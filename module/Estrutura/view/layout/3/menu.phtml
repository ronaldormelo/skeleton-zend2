<?=
        $this->navigation('navigation')
        ->setAcl($this->acl)
        ->setRole($this->role)
        ->menu()
        ->setPartial(array('layout/' . $this->Config('tema') . '/partials/menu.phtml', 'default'))
        ->render();
?>